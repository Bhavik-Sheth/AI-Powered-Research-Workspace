# Copy to `.env.development.local` (gitignored) to run the renderer
# standalone with `npm run dev`, outside the Electron shell.
# VITE_DEV_PORT: the port `python main.py` printed on startup.
# VITE_DEV_TOKEN: must match BEARER_TOKEN in backend/.env.
VITE_DEV_PORT=
VITE_DEV_TOKEN=devtoken
